const getters = {
	profile: state => state.profile,
	user: state => state.user,
	internals: state => state.internals,
	groups: state => state.groups,
	group_user: state => state.group_user,
	included: state => state.included,
}

export default getters
