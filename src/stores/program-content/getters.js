const getters = {
  contentList: state => state.contentList,
  update: state => state.update,
  newContent: state => state.newContent,
  typeListExist: state => state.typeListExist,
  typeListAll: state => state.typeListAll
};

export default getters;
