const getters = {
	count_approval_produk: state => state.count_approval_produk,
	produks: state => state.produks,
	tag_produks: state => state.tag_produks,
	tag_by_produk: state => state.tag_by_produk,
	tag_subproduks: state => state.tag_subproduks,
	included_produk: state => state.included_produk,
}

export default getters
