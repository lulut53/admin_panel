const mutations = {
	setManualNotifs(state, value) {
		state.manualNotifs = value
	},
	setPopupNotifs(state, value) {
		state.popupNotifs = value
	},
	setIncluded(state, value) {
		state.included = value
	},
}

export default mutations
